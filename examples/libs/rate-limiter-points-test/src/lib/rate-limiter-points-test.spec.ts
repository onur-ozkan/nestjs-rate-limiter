import { rateLimiterPointsTest } from './rate-limiter-points-test';

describe('rateLimiterPointsTest', () => {
  it('should work', () => {
    expect(rateLimiterPointsTest()).toEqual('rate-limiter-points-test');
  });
});
